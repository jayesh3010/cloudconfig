spring:
  application:
    name: ServiceB
  cloud:
    config:
      name:ConfigServer
      enabled:true
      uri:http://localhost:8888

server:
  port: 8091

management:
  security:
    enabled: false  
